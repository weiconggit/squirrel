package ${tablePackage}.${tableNameNoUnd};

import org.springframework.stereotype.Service;
import org.weicong.test.web.SysUser;
import org.weicong.test.web.SysUserMapper;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;

/**
 * <p>${tableNameCN} Service</p>
 * @author ${author}
 * @time   ${strutil.formatDate(lastupdata,"yyyy-MM-dd")} 
 * @version ${version}
 */
@Service
public class ${tableNameHump}Service extends ServiceImpl<${tableNameHump}Mapper, ${tableNameHump}VO>{
	
}
